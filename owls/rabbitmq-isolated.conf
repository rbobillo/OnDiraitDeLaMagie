[
 {rabbit,
  [
   %% The default "magic" user is only permitted to access the server
   %% via a loopback interface (e.g. localhost).
   %% {loopback_users, [<<"magic">>]},
   %%
   %% Uncomment the following line if you want to allow access to the
   %% guest user from anywhere on the network.
   {loopback_users, []},
   {default_vhost,       "/"},
   {default_user,        "magic"},
   {default_pass,        "magic"},
   {default_permissions, [".*", ".*", ".*"]}
  ]}
].